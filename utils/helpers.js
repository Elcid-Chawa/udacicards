import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingTop: 5,
    },
    item: {
        padding: 10,
        alignItems: 'center',
        justifyContent: 'center'

    },
    heading: {
        fontSize: 24,
    },
    subHeading: {
        fontSize: 12,
        color: 'gray'
    }
})